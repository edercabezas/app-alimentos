import { FechaNombrePipe } from './fecha-nombre.pipe';

describe('FechaNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
