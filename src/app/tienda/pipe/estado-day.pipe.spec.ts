import { EstadoDayPipe } from './estado-day.pipe';

describe('EstadoDayPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoDayPipe();
    expect(pipe).toBeTruthy();
  });
});
